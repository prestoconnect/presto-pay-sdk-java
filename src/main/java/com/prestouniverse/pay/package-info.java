/**
 * Presto Pay SDK — framework-agnostic client for Presto Connect payment APIs.
 *
 * <p>Public API lives in this package and in {@code payments}, {@code webhooks}, {@code crypto},
 * {@code http}, and {@code exception}. Types under {@code internal} are not part of the semver
 * guarantee; do not reference them from application code.
 */
package com.prestouniverse.pay;
