name: CI

on:
  push:
    branches: [master, main]
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        java: ["8", "11", "17", "21"]
    name: JDK ${{ matrix.java }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK ${{ matrix.java }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ matrix.java }}
          distribution: "temurin"
          cache: maven

      # Checkstyle needs Java 11+ to even load; the "checkstyle" profile (pom.xml) only activates
      # on JDK 11+, so plain `verify` already skips it correctly on JDK 8.
      - name: Verify (tests only)
        if: matrix.java == '8'
        run: ./mvnw --batch-mode verify

      - name: Verify (tests + Checkstyle)
        if: matrix.java != '8'
        run: ./mvnw --batch-mode verify

      - name: Build sources and Javadoc jars
        if: matrix.java == '17'
        run: ./mvnw --batch-mode verify -Prelease -DskipTests
